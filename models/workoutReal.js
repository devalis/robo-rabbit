class Workout {
  constructor(
    id,
    categoryId,
    title,
    description,
    date
  ) {
    this.id = id
    this.categoryId = categoryId
    this.title = title
    this.description = description
    this.date = date
  }
}

export default Workout
